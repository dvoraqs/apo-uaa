add email confirmation to registration
connect users and events through creator, organizer, and attendee fields
use facebook and possibly other services to log in
check if each page has a unique title
add description meta tags for each page
add breadcrumbs